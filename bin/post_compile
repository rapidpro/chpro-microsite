#!/bin/bash

#
# Deployment script for Heroku instances.
#

set -xeuf -o pipefail

yarn
manage.py migrate --database default --noinput

# Cleanup client side scripts which got already compiled
rm -Rf rapidpro_for_health/client/node_modules/
